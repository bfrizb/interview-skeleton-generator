<boilerplate_tips>

<challenge_description>


const is_palindrome = (word) => {
  return "<your_answer>";
}

var inputs = [
    <inputs>
];

inputs.forEach((word) => {
  console.log("input => " + word + " | output => " + is_palindrome(word));
});
