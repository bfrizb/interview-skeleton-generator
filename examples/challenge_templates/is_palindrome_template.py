<boilerplate_tips>

<challenge_description>


def is_palindrome(word):
    return "<your_answer>"


for word in [
    <inputs>
]:
    try:
        output = is_palindrome(word)
    except Exception as e:
        output = "Raised {}".format(repr(e))
    print("input = {} | output = {}".format(word, output))
